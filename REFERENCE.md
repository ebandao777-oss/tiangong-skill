# 天工.skill 运维参考手册

> 本文档为 SKILL.md 的补充内容，包含岗位型流水线细节、人物蒸馏方法论深度、质量体系运维参考、验证脚本文档、范式融合路径、异常处理等运维参考资料。Agent 运行时核心逻辑见 SKILL.md。

---

## 1. 岗位型专家流水线详细说明

### J1 需求萃取

**输入**：用户的一句话需求（如"帮我创建一个税务顾问智能体"）

**输出**：需求卡，包含以下字段：

| 字段 | 说明 | 示例 |
|------|------|------|
| 岗位 | 岗位名称 | 税务顾问 |
| 部门 | 所属职能域 | 专业服务 / 税务咨询 |
| 核心职责 | 3-5 条关键职责 | 税务筹划、合规咨询、风险评估、申报审核 |
| 已知交付物 | 用户期望的输出形式 | 税务分析报告、筹划方案 |
| 推荐 color | 按部门色标选择 | `#1A5276`（深海蓝） |

**门禁标准**：岗位名称明确、核心职责 ≥1 条。任一不满足 → 追问一个聚焦问题，不凭猜测推进。

**常见失败模式**：

| 失败 | 原因 | 一线修复 |
|------|------|---------|
| 用户描述过于模糊（如"做个有用的助手"） | 需求卡无法提取岗位名 | 追问岗位名称 + 核心职责 |
| 歧义（如"财务顾问"可能指税务/投资/审计） | 用户未指定子领域 | 列出 2-3 个方向让用户选择 |

---

### J2 模板填充

**输入**：J1 需求卡

**输出**：`examples/job-template.md` 12 模块完整骨架

**逐模块填充规则**：

| 模块 | 填充规则 | 缺失默认值 |
|------|---------|-----------|
| 身份与记忆（5 字段） | 角色 = `{领域} + {职能} + {限定词}` 三要素拼接 | 个性字段 ≥20 字 |
| 核心使命 + 反使命 | 使命用 `[动词]+[对象]+[交付标准]` 公式 | — |
| DO/DON'T 规则 | 按四组分类（合规/流程/质量/沟通） | 每组 3-5 条 |
| KPI 可量化 | ≥3 条，每条含数字或可验证标准 | — |
| 工作流 | 3-5 步覆盖关键路径 | — |
| 沟通风格 | 3 标签 + 拒绝方式 | 行业语气基调查表 |
| 退化行为设计 | 4 个极限场景（越界/信息不足/错误/崩溃） | — |

**必填字段门禁**：超过 2 个必填字段无法填充 → 回退 J1 补全需求卡。

**触发词设计规范**（J2 同步完成，5 维检查表见 [job-details.md](references/job-details.md) §触发词设计规范）：
1. 业务动词（设计/评估/分析/审查）
2. 对象名词（岗位名/领域名）
3. 交付暗示（帮我/输出/生成）
4. 快捷别名（用户可能的口语化表达）
5. 排除词（避免与系统保留词冲突）

---

### J3 技法注入

**输入**：J2 模板骨架

**输出**：充实后的高质量模板

**三层注入**：

| 层 | 注入内容 | 来源 | 核心要求 |
|----|---------|------|---------|
| 硬规则 | DO/DON'T 四组分类 | 岗位 SOP + 行业规范 | DO 每条必须包含"具体动作 + 为什么 + 反例"三要素 |
| 软技能 | 沟通风格 3 标签 + 拒绝方式 + 极限行为设计 | 行业语气基调查表 | 盲测可区分于其他岗位 |
| 领域知识 | 知识库三层（核心/应用/扩展）+ 知识图谱 | 三步注入法 | 可执行、可判定、可验证 |

**三步注入法**：
1. 识别领域知识类型（法规型 / 流程型 / 判断型）
2. 按岗位职责优先级注入（P0 > P1 > P2）
3. 验证可执行性（每条规则必须可操作、可判定、可验证）

**身份 5 字段填充标准**：

| 字段 | 要求 | 禁止 |
|------|------|------|
| 角色 | 领域 + 职能 + 限定词 | 仅有岗位名无领域限定词 |
| 个性 | ≥20 字，含性格描述 | "专业""严谨"等万能形容词堆砌 |
| 价值观优先 | 3-5 条排序，含触发场景 | 无优先级排序 |
| 记忆 | 用户偏好存储方式 | 留空 |
| 经验 | 领域年限 + 典型案例数 | "丰富"等无法量化的词 |

**常见失败模式**：

| 失败 | 原因 | 一线修复 |
|------|------|---------|
| J3 注入后身份 5 字段无显著差异 | 技法应用未针对具体岗位 | 逐个字段检查技法应用 |
| 表达DNA 场景锚点留空 | 未完成职业场景驱动的动态参数集 | 回退 J3 补充场景锚点 |

---

### J4 质量内控

**输入**：J3 充实后的模板

**输出**：通过/回退标记 + 质量评估报告

**岗位型专有 5 维评估**：

| 维度 | 检查内容 | 门禁标准 |
|------|---------|---------|
| 身份清晰度 | 5 字段是否完整、是否有性格而非仅名称 | 全部非空 + 个性字段 ≥20 字 |
| 规则可执行性 | DO/DON'T 每条是否含触发场景 + 具体行为 + 理据 | 100% 通过 |
| KPI 可量化 | 是否 ≥3 条、每条含数字或可验证标准 | ≥3 条含数字 |
| 沟通独特性 | 3 标签是否可区分于其他岗位、拒绝方式是否自然 | 盲测通过 |
| 规则一致性 | DO 之间、DON'T 之间、DO vs DON'T 无冲突 | 矩阵检测零致命冲突 |

**判定**：
- ≥4 维通过 = 合格，进入压力测试
- 2 维不通过 → 回退重填对应模块
- 3+ 维不通过 → 从 J2 重新开始

**压力测试**（2 个场景）：用边缘场景测试角色是否保持人设约束。

**自动化验证**：运行 `scripts/verify-skill.py`，退出码 0 方可进入 J5。

---

### J5 交付

**输入**：J4 通过标记

**输出**：完整 SKILL.md 写入 `output/` 目录 + 产出物声明（yyb-product）

**交付清单**：
- YAML frontmatter（name/description/version/domain/author）
- 全部 12 模块已填充
- verify-skill.py 退出码 0
- 5 维评估 ≥4 维通过
- 产出物路径已声明

---

## 2. 人物蒸馏流水线详细说明

### 蒸馏 0：需求澄清与意图解构

**目标**：禁止直接生成，先将需求拆解为可见步骤。

**输出**：需求卡，包含：
- 角色/人物名称明确
- 关键职责 ≥1 条
- 用户未表达的需求约束已确认
- 使用场景（决策辅助/创作/分析等）

**负向筛选（三条件）**：

| # | 淘汰条件 | 直接后果 |
|---|---------|---------|
| 1 | 一手来源 < 3 篇（著作/访谈/发言原文） | 心智模型无法通过三重验证 |
| 2 | 信息严重偏向单一领域 | 跨域复现证据缺失 |
| 3 | 该人物在核心问题上存在 3+ 处公开矛盾 | 心智模型无法自洽 |

命中任一条件 → 建议用户更换目标或降级为岗位型设计。

**输入安全**：角色名禁止包含 YAML 特殊字符（`---`、`:` 作为行首、`|`、`>`）和 Markdown 标题标记（`#` 行首），自动剥离。

---

### 蒸馏 1：多源信息采集

**目标**：并行调研 7 维信息维度。

| 维度 | 调研目标 | 揭示什么 |
|------|---------|---------|
| 著作与长文 | 书、论文、newsletter | 系统性思考、自创术语 |
| 对话与访谈 | 播客、长视频、深度采访 | 即兴思维、被追问时的反应 |
| 表达DNA | 社交媒体、短文 | 高频用词、句式偏好、幽默方式 |
| 他者视角 | 他人分析、批评、传记 | 外部观察到的模式、盲点 |
| 决策记录 | 重大决策、转折点 | 真实行为 vs 声称 |
| 失败与脆弱面 | 公开承认的错误、道歉声明 | 底层心理防御机制、自我修正模式 |
| 时间线 | 完整生平 | 关键里程碑、思想转折 |

**素材质量标准**（见 [extraction-framework.md](references/extraction-framework.md) §0）：

| 等级 | 定义 | 权重 | 最低门槛 |
|------|------|:--:|---------|
| 一手素材 | 本人著作、访谈原话、公开发言 | 3 | 深度蒸馏 ≥3 篇含 1 篇 ≥3000 字长文 |
| 二手素材 | 他人转述、传记、分析评论 | 2 | ≥5 篇 |
| 推断素材 | 基于上下文逻辑推导 | 1 | 不可独立支撑任何心智模型 |

**外部搜索降级策略**：某维度素材缺失 → 自动触发 web_search 搜索该人物公开著作、访谈、演讲原文。仍缺失 → 标记"外部来源待补充"。缺失 >3 维度 → 暂停。

**Gate 1 — 信息完备门禁**：
- 深度蒸馏：7 维度全部覆盖 + 一手来源占比 ≥30%
- 标准模式：5 维度覆盖 + 二手来源 ≥5 篇
- 精简模式：核心 3 维度覆盖
- 任一维度完全缺失 → 暂停提炼，优先补全调研

---

### 蒸馏 2：心智模型提炼

**目标**：从调研素材提取 3-7 个心智模型，通过四重验证筛选。

**设计原则**：长文 > 金句 · 争议 > 共识 · 变化 > 固定 · 意外但自洽

**四重验证**：

| 验证 | 判定问题 | 通过标准 | 未通过处理 |
|------|---------|---------|-----------|
| 跨域复现 | 在 ≥3 个不同场景中是否反复体现？ | 是 | 降级为"情境特例" |
| 生成力 | 能否预测对新任务的反应？ | 能 | 降级为"事后归因" |
| 排他性 | 换成同领域其他人是否还成立？ | 是（不成立则通过） | 标记为"行业共性"，移除 |
| 职业合规 | 是否符合职业强制性规范？ | 是 | 一票否决 |

**四重验证决策树**：
- 四重全部通过 → 心智模型
- 仅 1-3 重通过 → 降级为决策启发式
- 0 重通过 → 丢弃
- 职业合规一票否决 → 无条件移除

**同步提取**：
- 决策启发式（5-10 条）：表述为"如果 X，则 Y"，有具体案例支撑。反向验证：每条必须能追溯到某个心智模型
- 表达 DNA：句式偏好、高频词汇、幽默方式、确定性风格。无素材标注"未确认"
- 价值观与反模式：价值排序 + 明确反对的行为/思维方式
- 内在张力：价值观之间的冲突 ≥1 个方向有具体案例
- 诚实边界：5 条必须全部确认 + 一手来源占比已计算

**Gate 2 — 提炼质量门禁**：
- 心智模型 3-7 个（四重验证全部通过）；<3 → 回退蒸馏 1；>7 → 截断
- 决策启发式 5-10 条（每条可追溯到心智模型）
- 表达 DNA 各方法均有内容或标注"未确认"
- 诚实边界 5 条全部确认 + 一手来源占比已计算；<50% → 标注"低可信度"
- 内在张力 ≥1 个方向有具体案例（或标注"未观察到"）

---

### 蒸馏 3：结构化构建

**目标**：将提炼内容转化为可执行 Prompt 结构。

**核心产物**：按 `references/persona-details.md` 完整模板输出的 SKILL.md，核心模块链：
角色扮演规则 → 身份卡 → 价值观与反模式 → 心智模型 → 决策启发式 → 回答工作流 (Agentic Protocol) → 表达DNA → 人物时间线 → 智识谱系 → 诚实边界 → 技术交付物 → 工作流程 → 成功指标 → 沟通风格 → 退化行为设计 → 附录：调研来源

**元数据配置**：
- `name`: 简洁有力
- `description`: 一句话价值主张（角色 + 关键能力 + 交付物）
- `color`: 人格型用紫色系 `#8E44AD`

**Agentic Protocol 生成**（人格角色型专属）：
- 问题分类：事实/纯框架/混合三类路由规则
- 研究维度推导：从心智模型反推人物分析问题的搜索方向
- Step 1→2→3 闭环：分类 → 搜索 → 用心智模型输出

**门禁**：所有关键模块已定义、无遗漏必填字段、无规则冲突。

---

### 蒸馏 4：质量验证

**目标**：内部质检，提升交付可信度。

**两轮评估**：
1. 覆盖完整性 + 边界条件（硬指标，verify-persona.py 覆盖）
2. 实际可用性 + 表述清晰度 + 输出稳定性（软指标）

**第 2 轮检查清单**：

| 检查项 | 方法 | 不通过标志 |
|--------|------|-----------|
| 输出稳定性 | 对同一输入跑 2 次对比核心结论 | 不一致 >20% |
| 指令可执行性 | 模拟执行每条 DO 规则 | 任一条无法模拟执行 |
| 角色一致性 | 用 2 个边缘场景检查是否偏离人设 | 出现"作为 AI 助手"等通用话术 |
| 交付物完整度 | 核对 16 必选模块 | 任一模块为空或占位符 |

全部 4 项通过 = PASS。1 项不通过 → 回退修复对应模块。2 项以上不通过 → 回退蒸馏 3 整体审校。

**融合可信度测试**：按 [extraction-framework.md](references/extraction-framework.md) §内容完整性清单与注入测试 执行。

**Gate 3 — 交付就绪门禁**：
- `verify-persona.py` PASS
- 压力测试 4 场景全部通过
- 置信度 <70% 的关键断言已撤回或降级为"推测"

---

### 蒸馏 5：交付与反馈闭环

**交付内容**：
- 完整 SKILL.md（含 YAML frontmatter + 全部 16 章节）
- 写入 `output/` 目录
- 附带 2-3 个后续操作建议
- 产出物声明（yyb-product）

---

## 3. 质量体系运维参考

### 3.1 规则冲突检测矩阵

DO/DON'T 规则组可能含隐性矛盾。检测操作步骤：

1. 列出所有 DO 规则 → 编号 D1, D2, D3...
2. 列出所有 DON'T 规则 → 编号 N1, N2, N3...
3. 逐对交叉检查：D1×N1, D1×N2, ..., D1×D2, ..., N1×N2, ...
4. 每条冲突标注：冲突类型 + 修复建议
5. 输出冲突检测报告

**三模式完整矩阵**：

| 检查模式 | 示例冲突 | 修复方法 |
|---------|---------|---------|
| DO vs DON'T | DO: "所有回答须有数据支撑" + DON'T: "禁止主动搜索外部数据" | DON'T 改为"搜索前须用户确认" |
| DO vs DO | DO: "回复必须详细完整" + DO: "回复必须简洁在 100 字以内" | 标注主 DO + 例外 DO |
| DON'T vs DON'T | DON'T: "禁止拒绝用户请求" + DON'T: "禁止执行危险操作" | 加边界规则 |

**严重度分级**：
- 🔴 致命：两条规则互斥，角色永远无法同时遵守
- 🟡 严重：两条规则存在显著张力，特定场景下冲突
- 🟢 轻微：边缘场景下的潜在矛盾，概率 <5%

---

### 3.2 反模式与禁止清单

以下行为在蒸馏全流程严格禁止：

| # | 禁止行为 | 检测方式 | 违规后果 |
|---|---------|---------|---------|
| 1 | 编造此人没说过的话 | 对照调研素材逐句核查 | 回退蒸馏 1 补素材 |
| 2 | 包装通用道理为独特见解 | 检查每条启发式是否通过四重验证 | 删除该条目，重新提炼 |
| 3 | 信息不足时强行生成 | 检查每项产出是否有素材支撑 | 缺失项标注"未确认" |
| 4 | 心智模型不足时宁少勿多 | 心智模型 <3 且无一通过四道验证 | 回退蒸馏 1 |

---

### 3.3 反膨胀检查清单

| # | 检查项 | 膨胀信号 | 健康标准 |
|---|--------|---------|---------|
| 1 | 使命能否用一句话说清？ | 写了 3 个以上互不重叠的使命 | 1-2 个 |
| 2 | DO/DON'T 是否超过 10 条？ | >10 条 | 每组 3-5 条 |
| 3 | 工作流是否超过 5 步？ | >5 步 | 3-5 步 |
| 4 | 是否添加了不需要的高级能力？ | 简单岗位被塞入心智模型 | 只写当前真正需要的 |
| 5 | KPI 数量是否超过角色复杂度？ | 设 5 个 KPI | 岗位型 2-3 个、人格型 1-2 个 |
| 6 | 输出信息密度是否足够？ | 核心信息 <60% | 核心信息 ≥60% |

---

### 3.4 失败回退策略

完整回退协议见 SKILL.md §统一回退协议。关键场景速查：

| 触发条件 | 一线修复 | 仍失败兜底 |
|---------|---------|-----------|
| 用户需求过于模糊 | 追问 1 个聚焦问题 | 基于最常见场景给出默认设计 |
| 结构化构建找不到合适模板 | 从关键模块定义表最接近组合 | 降级为自由格式输出 |
| 多维评估不满足验收标准 | 回退阶段二修复短板模块 | 标记"已知限制"写入交付说明 |
| 用户对交付物不满意 | 定位不满意的具体模块，单模块重设计 | 完整重新设计，保留前版本 |
| 输出超长导致截断 | 优先保留关键模块 | 分两次输出 |
| 用户要求修改已有角色 | 定位受影响模块，靶向编辑 | 保留旧版本备份 |
| 缺少可量化 KPI | 从行业报告提取基准指标 | 写入"建议运行 2 周后根据实际数据校准" |

---

## 4. 验证脚本详细文档

### verify-skill.py — 通用结构验证

```bash
python scripts/verify-skill.py [--skill path/to/SKILL.md] [--json] [--quiet]
```

**退出码**：`0` = PASS, `1` = NEEDS_REVIEW

**7 大维度**：

| 维度 | 内容 | 通过标准 |
|------|------|---------|
| YAML frontmatter | name/description/version/domain/author | 五字段齐全 |
| Gate 标记 | CHECKPOINT 分布与行号 | 3 个硬门禁全部存在 |
| H2 标题统计 | 蒸馏流程/设计模式/工具类标题 | 覆盖完整 |
| 规则冲突检测 | DO vs DON'T 语义交叉矩阵 | 零致命冲突 |
| 反模式索引 | 不触发条件（4 条）+ 蒸馏红线（5 条） | 全部可校验 |
| 引用文件校验 | 代码围栏配对 + H2 去重 + 最低 100 字符 | 全部通过 |
| 岗位型深度结构 | 11 项检查（条件激活：仅当检测到"身份与记忆"章节） | 11 项全 PASS |

**岗位型深度结构 11 项**：

| # | 检查项 | 通过标准 |
|---|--------|---------|
| 1 | 身份 5 字段 | 角色/个性/价值观优先/记忆/经验 全部非空 |
| 2 | 使命反使命 | 使命 + 反使命均已定义 |
| 3 | 工作流 3 步 | 步骤数 [3, 5] |
| 4 | 流派共识 | 已定义 |
| 5 | 交付物 | 已定义 |
| 6 | 步骤数范围 | [3, 5] |
| 7 | 沟通风格 3 标签 | 已定义且 ≥3 个标签 |
| 8 | 极限行为 4 场景 | 越界/信息不足/错误/崩溃 四个场景均已定义 |
| 9 | KPI ≥3 | 已定义且 ≥3 条 |
| 10 | 知识库 3 层 | 核心/应用/扩展 均已定义 |
| 11 | 诚实边界 ≥5 | 已定义且 ≥5 条 |

**规则冲突检测内部逻辑**：
1. 关键词预筛 — 从 DO/DON'T 规则中提取动作词
2. 语义分析 — 检测互斥语义（如"搜索" vs "禁止搜索"）
3. 冲突报告 — 输出配对 + 冲突描述 + 严重度 + 修复方案

---

### verify-persona.py — 人格蒸馏专用验证

```bash
python scripts/verify-persona.py <path/to/SKILL.md> [--json]
```

**退出码**：`0` = PASS, `1` = FAIL

**16 项检查清单**：

| # | 检查项 | 通过标准 |
|---|--------|---------|
| 1 | YAML frontmatter (name) | 非空 |
| 2 | YAML frontmatter (description) | 非空 |
| 3 | YAML frontmatter (color) | 紫色系 #8E44AD 或有效 hex |
| 4 | 16 章节全覆盖 | 全部存在 |
| 5 | 身份卡 5 字段 | 角色/个性/价值观优先/记忆/经验 非空 |
| 6 | 心智模型 [3, 7] | 含三重验证痕迹（跨域复现/生成力/排他性） |
| 7 | 决策启发式 [5, 10] | 每条含场景映射 |
| 8 | 表达 DNA 7 维度 | 句式/语气/高频词/确定性/幽默/情绪/姿态 |
| 9 | 回答工作流 3 步 | 分类→搜索→用心智模型输出 |
| 10 | 诚实边界 ≥5 | 含预测局限/不能替代创造力/公开vs真实差距/信息截止/一手来源占比 |
| 11 | 退化行为 4 场景 | 越界/信息不足/错误/崩溃 |
| 12 | 工作流程 [3, 5] | 步骤数合规 |
| 13 | 成功指标 ≥3 | 已定义 |
| 14 | 技术交付物 ≥1 | 已定义 |
| 15 | 调研来源 | 一手+二手 ≥1 个 |
| 16 | 人物时间线/沟通风格/智识谱系 | 均已定义 |

**额外检查**：一手来源占比 < 30% → 警告 "⚠️ 低可信度"（不阻断 PASS，仅警告）

---

## 5. 范式融合路径详细说明

### 何时融合

用户同时需要岗位能力 + 人物思维风格时启用。

### 融合流程

```
Step 1: 岗位型 J1→J5 完整流水线
  → 产出：完整的岗位能力 SKILL.md（12 模块）

Step 2: 人格蒸馏 蒸馏 0→2
  → 产出：目标人物的心智模型（3-7 个）+ 决策启发式（5-10 条）+ 表达 DNA

Step 3: 技法注入（替换 J3）
  → 将蒸馏 2 的产出以 "思维风格注入" 形式融入岗位型 SKILL.md
  → 心智模型 → 补充到"认知思考协议"模块
  → 决策启发式 → 补充到 DO/DON'T 规则（标注"人物风格"前缀）
  → 表达 DNA → 替换沟通风格模块

Step 4: 融合冲突检测
  → 检查人物风格 DO 是否与岗位合规 DON'T 冲突
  → 存在冲突 → 岗位合规优先，人物风格降级为"参考建议"
  → 输出融合冲突报告

Step 5: J4 质量内控 + verify-skill.py
```

### 融合冲突检测表

| 冲突类型 | 处理策略 |
|---------|---------|
| 人物启发式 vs 岗位合规 | 合规优先，启发式标注"在合规前提下" |
| 人物表达风格 vs 岗位沟通规范 | 取交集：保留人物语气基调，替换不符规范的具体话术 |
| 人物价值观 vs 岗位伦理 | 岗位伦理一票否决，冲突价值观移除 |

---

## 6. 平台兼容性与能力边界

### 平台兼容性

| 平台 | 状态 | 说明 |
|------|------|------|
| Windows | 完全支持 | Python ≥3.8；PowerShell 5.1 |
| Linux | 完全支持 | Python ≥3.8；Bash |
| macOS | 完全支持 | Python ≥3.8；Homebrew Python 需确保 pip 包在 PATH |

**脚本约束**：
- 所有脚本使用 pathlib.Path 自动适配路径分隔符
- verify-skill.py / verify-persona.py 通过 `_common.py` 共享工具模块
- 规则冲突检测为纯 Python 实现，零外部依赖

### 能力边界

| 支持 | 不支持 |
|------|--------|
| 人格蒸馏：基于真实人物的著作/访谈/公开表达 | 虚构人物或用户想象的角色（走岗位型） |
| 岗位型：任何有明确职责和交付标准的职业角色 | "万能助手"类无领域限定词的通用 Agent |
| 范式融合：先岗位型后注入人物风格 | 先人物蒸馏后追加岗位规则的逆序融合 |
| 单角色设计/优化/评估 | 多角色协作编排（需多次独立调用） |

---

## 7. 异常处理与故障排查

### 蒸馏失败场景

| 场景 | 症状 | 精确处理 |
|------|------|---------|
| 素材严重不足 | 一手来源 <3，二手 <5 | 建议换人或降级。详见 [FAQ #1](references/faq.md) |
| 心智模型无法通过四重验证 | 蒸馏 2 产出为空 | 回退蒸馏 1 补充调研维度；仍不足 → 评估人物是否适合蒸馏 |
| 表达 DNA 多方法缺失 | 句式/语气/高频词等无素材 | 标注"未确认"；多方法缺失 → 评估素材充分性 |
| verify-persona.py FAIL | 16 项中有不通过 | 按报错逐项修复；仍 FAIL → 退回蒸馏 3 重新构建 |

### 校验不通过场景

| 场景 | 症状 | 精确处理 |
|------|------|---------|
| verify-skill.py NEEDS_REVIEW (1) | YAML 字段缺失/规则冲突/反模式命中 | 按报错定位问题模块，回退对应的设计阶段修复 |
| 岗位型深度结构不通过 | 11 项中有不通过 | 回退 J2 重填对应模块 |
| 压力测试场景不通过 | 角色在边缘输入下崩人设 | 回到蒸馏 2 重新审视价值观排序和决策启发式 |
| 规则冲突致命 | DO vs DON'T 存在致命冲突 | 保留 DON'T 规则，弱化冲突的 DO 规则；冲突无法消解 → 输出两版方案 |

### 验证脚本不可用时的回退

| 脚本不可用 | 回退方案 |
|-----------|---------|
| verify-skill.py | 手动逐条核对 YAML 字段 / CHECKPOINT 标记 / 规则冲突矩阵 |
| verify-persona.py | 手动逐条核对 16 项检查清单 |
| 规则冲突检测 | 人工矩阵检测：逐对交叉（D1×N1, D1×N2...），输出冲突类型 + 修复建议 |

### 迭代黄金法则

能改一行不改一段，能改一段不改一页。保留前版本备份。

| 迭代需求 | 定位方法 | 编辑范围 |
|---------|---------|---------|
| 加幽默感 | 修改沟通风格模块 | 仅该模块 |
| KPI 调整 | 修改成功指标模块 | 仅该模块 |
| 追加规则 | 在对应组别下追加 | 仅该规则组 |
| 扩展能力 | 检查是否在现有使命范围内 | 使命 + 交付物 |
| 换人物风格 | 启动蒸馏流程替换身份字段 | 身份与记忆 + 心智模型段 |
| 整体重来 | 不改增量，走完整流程 | 全文件重写 |

---

## 8. 更新记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v3.0 | 2026-06 | 重构质量体系：拆分 quality-system.md 为三独立文件；引入规则冲突检测引擎；新增反模式清单独立章节；统一检查点标记为 🚦 等级体系；README.md 完整重写；REFERENCE.md 新建 |
| v2.x | 2026-05 | 新增范式融合路径；岗位型深度结构校验 11 项；触发词五维检查表 |
| v1.x | 2026-04 | 初始版本：两种范式、J1→J5 流水线、verify-skill.py / verify-persona.py |
